= add(x, y) =
= sub(x, y) =
= mul(x, y) =
= div(x, y) =
= pow(x, y) =

 These built-ins will perform their respective mathematical operations. These work on any combination of float and
 integers, with the exception of add(x, y), which works on strings as well.
